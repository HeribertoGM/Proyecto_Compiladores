program Test1;
let i, j, p, arreglo[10]: int;
let x, y, z[3]: float;

function int fact(int j)
let h: int;
{
    i = j + (p - j * 2 + j);
    if (j == 1) {
        return(j);
    }
    else {
        return(j * 2);
    }
}

function void calcula(int y, float k)
let g: float;
{
    x = 1;
    while (x < 11) {
        y = y * arreglo[x];
        x = x + 1;
        write(arreglo[x]);
    }
    write("acumulado", y);
}

main() {
	read(p);
    j = p * 2;
    i = p * p;
    for i = 1 to 10{
        arreglo[i] = p + i;
    }
    p = 5;
    for i = 1 to p{
        arreglo[i] = p + i;
    }
    while (i > 0){
        calcula(p - i);
    }
}