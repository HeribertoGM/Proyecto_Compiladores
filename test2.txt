program Test2;
let i, j, p, arreglo[10]: int;
let valor: float;

function int fact(int j)
let i: int;
{
    i = j + (p - j * 2 + j);
    if (j == 1) {
        return(j);
    }
    else {
        return(j * 2);
    }
}

function void calcula(int y)
let x: int; 
{
    x = 1;
    while (x < 11) {
        y = y * arreglo[182];
        x = x + 1;
        write(arreglo[x][asd+u3][12+21]);
    }
    write("acumulado", y);
}

main() {
    read(p);
    j = p * 2;
    i = p * p;
    for i = 1 to 10 {
        arreglo[i] = p + i;
    }
    p = 5;
    while (i > 0) {
        calcula(p - i);
        j = 4;
        write(j, i);
        i = i + 1;
    }
}
